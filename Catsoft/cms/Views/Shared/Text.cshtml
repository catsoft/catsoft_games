@inject TextResourceRepository Repository
@using App.cms.Repositories.TextResource
@model TranslateViewModel
@{
    Layout = null;
}
@if (Model.Translate)
{
    @((await Repository.GetByTag(Context, Model.Tag)).Trim());
}
else
{
    @Model.Tag.Trim();
} 